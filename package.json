{
  "name": "nexus-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx -y concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev --prefix nexus-backend\" \"npm run dev --prefix nexus-frontend\"",
    "dev:backend": "npm run dev --prefix nexus-backend",
    "dev:frontend": "npm run dev --prefix nexus-frontend",
    "build": "npm run build --prefix nexus-frontend && npm run build --prefix nexus-backend",
    "install:all": "npm install --prefix nexus-backend && npm install --prefix nexus-frontend"
  }
}
